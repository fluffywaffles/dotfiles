#!/bin/bash

LEFT_MARGIN=100
BOTTOM_MARGIN=200

place_quote_block() {
    QUOTE_X=$(( MONITOR_X + LEFT_MARGIN ))
    QUOTE_Y=$(( MONITOR_Y + MONITOR_HEIGHT - BLOCK_HEIGHT - BOTTOM_MARGIN ))
    CITATION_X=$QUOTE_X
    CITATION_Y=$(( QUOTE_Y + QUOTE_HEIGHT + CITATION_GAP ))
}
