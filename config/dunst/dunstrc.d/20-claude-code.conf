# Claude Code notifications keep the normal palette; urgency alone separates something waiting on
# an answer from a finished turn. None of them expire on their own, whatever their urgency, so
# nothing is missed by being away from the screen. One notification per pane, via a stack tag.

[claude-code]
    appname = "claude-code"
    timeout = 0
